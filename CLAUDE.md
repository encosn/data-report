# data-report — 데이터 탐구 수행평가 (수집·시각화·분석)

> 한 줄 소개: **데이터를 수집하고, 그래프로 시각화하고, 의미를 해석하는 5단계 수행평가 위저드. 완성한 보고서를 파일로 내보내 구글 클래스룸에 직접 제출한다.**

## ① 목적과 대상 단원

- **단원**: 중학교 정보 2단원 **데이터**
- **성취기준**
  | 코드 | 내용 | 앱에서 다루는 곳 |
  |:---|:---|:---|
  | [9정02-02] | 문제 해결에 적합한 데이터를 수집하고, 목적에 맞게 구분하여 관리한다 | 2단계 데이터 수집 |
  | [9정02-03] | 실생활의 데이터를 표, 다이어그램 등 다양한 형태로 구조화한다 | 3단계 데이터 시각화 |
  | [9정02-04] | 사례를 중심으로 데이터 간의 관계를 파악하고, 데이터에 기반하여 의미를 해석한다 | 4단계 데이터 분석 |
- **[비상교육] 중학_정보_2_교사용 교과서** II단원 "2. 데이터 분석"의 흐름(문제 정의 → 데이터 수집 및
  구조화 → 그래프로 표현하기 → 데이터 의미 해석)을 그대로 따랐다. 교과서는 이 과정에 외부 웹 도구
  **코답(CODAP, codap.concord.org)**을 쓰지만, 이 앱은 그래프를 앱 안에서 직접 그린다(아래 "왜 CODAP을
  임베드하지 않았는가" 참고). 수집 방법 목록(설문조사·관찰·인터뷰·센서·공공데이터 사이트)과 분석 가이드
  질문은 교과서의 "정리하기"·"데이터 의미 해석" 예시를 참고해 각색했다.
- **구글 클래스룸 제출은 "보고서 파일 내보내기"로 구현했다** — 앱이 직접 Classroom API로 제출하지
  않는다. 사용자에게 세 가지 방식(① 파일 내보내기 ② 구글 로그인+드라이브 저장 ③ Classroom API 자동
  제출)을 제시했고 ①을 골랐다. 이유: ②③은 학생의 구글 로그인(OAuth)이 필요해 "학생 개인정보를
  수집·저장하는 기능은 만들지 않는다"는 프로젝트 규칙과 정면으로 부딪히고, Google Cloud 프로젝트·OAuth
  동의 화면 등 외부 설정이 앱 코드 밖에서 별도로 필요하다. ①은 로그인이 전혀 없고 정적 웹앱 그대로
  유지된다 — 학생이 내려받은 보고서 파일(html)을 클래스룸 과제 화면에서 직접 첨부해 제출한다.

## ② 기능 목록

| 단계 | 기능 |
|:---|:---|
| 0 시작하기 | 성취기준 3개 안내, 5단계 진행 순서 미리보기, "이어서 하기"(진행 중이던 내용이 있으면) |
| 1 문제 정의 | 탐구 질문 입력(자유 텍스트) + 예시 4개 보기 |
| 2 데이터 수집 | 수집 방법 체크(설문조사·관찰·인터뷰·센서·공공데이터·기타, 복수 선택) + 출처 기록 |
| | [파일 업로드] CSV/xlsx/xls 업로드 → SheetJS로 파싱, CP949/UTF-8 인코딩 자동 판별, **병합된 셀 자동 펴기**, 열 자료형(텍스트/숫자) 자동 추정 |
| | ↳ **「헤더 확인」 별도 화면** — 파일을 올리면 곧바로 표가 되지 않고 이 화면으로 먼저 간다. 미리보기(열 전체 · 행 최대 20개)를 보며 학생이 **두 가지를 직접 지정**한다 |
| | &nbsp;&nbsp;· **헤더로 쓸 줄(행) 수** ⬇️ — 위에서부터 N줄을 `1>2>3` 순서로 이어 붙여 열 이름을 만든다(예: `2023년>상반기>방문객수`). 세로 병합으로 같은 값이 반복되면 한 번만 쓴다 |
| | &nbsp;&nbsp;· **헤더로 쓸 칸(열) 수** ➡️ — 왼쪽 M칸을 "항목 이름" 열로 본다. **생략되어 빈 칸으로 남은 값을 위에서 이어 채우고**(캐리포워드), 숫자처럼 보여도 자료형을 텍스트로 둔다. 열을 합치지는 않는다 |
| | &nbsp;&nbsp;· 미리보기에서 헤더 줄은 청록, 헤더 칸은 주황, **이어 채워 준 값은 청록 글씨**로 구분해 보여 준다 |
| | [표에 직접 입력] 열 이름·자료형·값을 직접 입력하는 편집 가능한 표, 열·행 추가/삭제 (**행 삭제 버튼은 각 행의 맨 왼쪽 칸**에 있다) |
| | **원하는 것만 골라 넘기기** — 열 머리마다 「이 열 쓰기」 체크박스, 행마다 왼쪽 끝 체크박스. **체크한 열·행만 3단계로 넘어간다.** 행 검색창(아무 칸의 값으로 찾음) + 「보이는 행 모두 체크/해제」(검색으로 걸러진 것에만 적용). 체크를 끄는 것은 **지우는 게 아니라서** 되돌아오면 원본 표가 그대로 있다 |
| | 두 방식 모두 같은 편집 그리드를 함께 쓰므로 업로드 후에도 값을 고치거나 열을 추가할 수 있다 |
| 3 데이터 시각화 | 그래프 종류 4종(막대·꺾은선·산점도·원) 중 선택, 가로축·세로축 열 선택(자료형에 따라 고를 수 있는 열이 자동으로 걸러진다), 막대그래프는 집계 방식(평균/합계/개수) 추가 선택. Chart.js로 실시간 렌더링 |
| 4 데이터 분석 | 그래프를 다시 작게 보여주며 가이드 질문 4개(가장 크게/작게·관계·이유·주장)에 서술형으로 답변, 문항마다 힌트 보기 |
| 5 보고서 | 전체 내용(질문·수집방법·표·그래프·분석답변·성취기준)을 미리보기로 확인, 학번·이름 입력(저장되지 않음, 아래 "개인정보" 참고), 보고서 파일(html, 그래프 이미지 포함 자체완결형) 다운로드, 인쇄(PDF로 저장), 구글 클래스룸 새 탭 열기, 처음부터 다시 하기 |
| 공통 | 모든 진행 내용은 `localStorage`(`data-report-state-v1`)에만 저장 — 서버 전송·로그인 없음. 진행 표시줄에서 이미 지나온 단계로 바로 이동 가능 |

## ③ 현재 개발 상태와 다음 할 일

- **1차 완성 (2026-08-03)** · 포트 8087 · 단위 시험 203개 통과 (`npm test` — util/state/select/header-merge/io/chart/report 7개 파일)
- 실제 브라우저에서 전체 흐름(주제 입력 → 수집 방법 체크 → 표 직접 입력으로 5행 데이터 작성 → 막대그래프
  시각화 → 분석 4문항 작성 → 보고서 확인)을 끝까지 수동으로 확인했다. 확인한 것: 텍스트 입력 중 포커스가
  끊기지 않는지, 열/행 추가·삭제, 그래프 실시간 갱신(canvas에 Chart.js 인스턴스 정상 생성), 보고서 파일
  다운로드(blob 생성 확인, 그래프 base64 이미지·질문·표·분석답변·성취기준 모두 포함됨을 fetch로 검증),
  구글 클래스룸 새 탭 열기, `localStorage` 저장 후 새로고침해도 내용 유지, 진행 표시줄로 이전 단계
  재방문, "이어서 하기" 안내 문구 노출.
- 실제 교실 사용은 아직 안 해 봤다. 수업 뒤 피드백으로 질문 문구·힌트를 다듬을 것.
- 한 것
  - [x] `hub/data.js` 2단원 `activities` 에 항목 추가 (status: 'soon' — 배포 후 url 채우고 'ready'로)
- 아직 안 한 것
  - [ ] GitHub 저장소 만들고 Actions 배포 (`.github/workflows/deploy.yml` 없음, `binary-converter`의 것을 복사)
  - [ ] `dist/` 빌드 결과 실제 브라우저 확인 (개발 서버로는 확인함, `npm run build`는 성공)
  - [ ] 파일 업로드 경로를 실제 공공데이터 CSV(KOSIS 등)로도 한 번 확인 (지금은 표 직접 입력 경로만 수동 확인)

---

# 여기부터는 고치기 전에 꼭 읽을 것

## 왜 CODAP을 임베드하지 않았는가

교과서는 코답(CODAP, codap.concord.org)이라는 외부 웹 도구로 표·그래프를 다룬다. 이 앱은 그 대신
Chart.js를 앱 안에 직접 넣어 그래프를 그린다. **일부러 이렇게 했다.**

1. **임베드 가능 여부가 불확실하다.** CODAP을 iframe으로 넣으려면 그 사이트가 프레임 삽입을
   허용해야 하는데(X-Frame-Options/CSP frame-ancestors), 확인되지 않은 외부 사이트에 의존하면
   수행평가 도중 갑자기 안 열릴 위험이 있다.
2. **개인정보·상태 관리가 더 단순하다.** 자체 제작하면 그래프 설정과 데이터가 이 앱의 state 안에
   그대로 남아 보고서에 바로 포함시킬 수 있다. 외부 도구를 쓰면 학생이 캡처하거나 다시 옮겨 적어야 한다.
3. **채점 가능한 보고서를 자동으로 만들 수 있다.** canvas.toDataURL()로 그래프를 그대로 이미지화해서
   보고서 파일에 넣는다 — 외부 도구였다면 이 자동화가 불가능했다.

→ **"진짜 코답을 쓰고 싶다"고 iframe으로 바꾸지 말 것.** 대신 코답을 실제로 써 보고 싶은 학생을 위해
나중에 "코답에서 열어보기(새 탭)" 버튼 정도를 추가하는 선택지는 있다.

## 왜 구글 클래스룸에 직접 제출하지 않는가

`step5-report.js`의 "구글 클래스룸 열기" 버튼은 `window.open('https://classroom.google.com')`으로
새 탭을 여는 것뿐이다. Classroom API로 자동 제출하지 않는다. **일부러 이렇게 했다** — 사용자에게
세 가지 선택지(파일 내보내기 / 구글 로그인+드라이브 저장 / Classroom API 자동 제출)를 제시했고
"파일로 내보내기(권장)"를 선택했다.

- Classroom API로 제출하려면 학생이 자기 구글 계정으로 로그인해야 하는데, 이는 프로젝트 규칙
  "학생 개인정보를 수집·저장하는 기능은 만들지 않는다"와 정면으로 부딪힌다.
- Google Cloud 프로젝트·OAuth 동의 화면 등 **앱 코드 밖에서** 별도 설정이 필요해 이 앱만으로는
  끝나지 않는다.
- 파일 내보내기 방식은 로그인이 전혀 없고, 정적 웹앱(로그인·백엔드 없음) 그대로 유지된다.

→ **"제출까지 자동으로 되면 편할 텐데" 하고 구글 로그인을 넣지 말 것.** 넣으려면 먼저 사용자에게
   위 트레이드오프(개인정보 규칙 위반 가능성, Google Cloud 설정 필요)를 다시 확인해야 한다.

## 왜 헤더 줄 수를 자동으로 추정하지 않는가

공공데이터포털에서 받은 파일은 열 이름이 1줄이 아니라 2~3줄이거나 셀이 병합된 경우가 흔한데,
**그 모양이 파일마다 전부 다르다.** 자매 앱 `data-cleaner`(`lib/parse.js`)는 세로 병합 높이·글자/숫자
대비 등 여러 근거에 점수를 매겨 헤더 줄 수를 자동으로 추정하지만, 이 앱은 **일부러 그 로직을 가져오지
않았다** (2026-08-03 사용자와 상의해 결정).

이유:
1. **자동 추정은 자주 틀린다.** 틀렸을 때 학생은 왜 열 이름이 이상한지 알 수 없다.
2. **`parse.js`급 휴리스틱은 200줄이 넘는다.** 이 앱이 노린 단순함을 잃는다.
3. **직접 고르는 것이 교육적으로 낫다.** "이 표에서 어디까지가 열 이름인가"를 학생이 판단하는 것
   자체가 데이터 구조를 읽는 연습이다.

대신 `lib/header-merge.js`에 **순수 함수 세 개만** 두었다 — `unmergeGrid()`(병합된 칸을 값으로 펴기),
`carryForwardCols()`(헤더 열의 생략된 값 이어 채우기), `mergeHeaderRows()`(고른 줄 수만큼 `1>2>3`으로
이어 붙이기). 추정은 하지 않고 시키는 대로만 한다.

### 헤더 "열"은 왜 필요하고, 왜 합치지 않는가 (2026-08-04 추가)

헤더는 여러 **줄**만 있는 게 아니라 왼쪽 여러 **칸**이 항목 이름인 경우도 흔하다(시도 | 시군구 | 값…).
그리고 한국 통계표는 **같은 값이 이어질 때 아래 칸을 비워 두는 습관**이 있다:

```
시도  시군구  인구
서울  종로    100
      중구     90     ← "서울"이 생략됨 (사람 눈에는 여전히 서울)
부산  중구     70
```

이걸 그대로 두면 시도로 막대그래프를 그렸을 때 서울 막대 하나에 빈 칸이 잔뜩 생긴다. 그래서
**헤더 열로 지정한 칸만** 위 값을 이어 쓴다(`carryForwardCols`). 오른쪽의 실제 측정값 열은 건드리지
않는다 — 그건 진짜 결측치일 수 있다. 이 캐리포워드 아이디어와 "헤더 행 수 + 헤더 열 수를 직접 고른다"는
설계는 자매 앱 **`excel-picker`**(`lib/xlsx-core.js`의 `carryForwardIdCols`)에서 가져왔다.

`excel-picker`와 다른 점 하나: **헤더 열들을 하나로 합치지 않는다.** `excel-picker`는 행 이름표를
`서울 > 종로`처럼 합쳐 보여 주지만(체크박스 목록용), 이 앱은 시도·시군구를 **따로 둔다** — 그래야
학생이 "시도별로 볼까, 시군구별로 볼까"를 골라 가로축에 쓸 수 있다. 합쳐 버리면 그 선택지가 사라진다.

> `carryForwardCols`는 **상위 열의 값이 바뀌면 오른쪽(하위) 열의 이어쓰기를 초기화**한다.
> 위 예에서 "부산"이 새로 나왔는데 시군구가 비어 있다면, 앞의 "중구"를 잘못 물려주지 않고 빈 칸으로 둔다.

### 이 세 곳은 건드릴 때 특히 조심할 것 (2026-08-04 적대적 검토로 찾아 고친 결함)

1. **빈 행 판정은 반드시 `src`(원본)로 한다.** `mergeHeaderRows`는 캐리포워드를 먼저 돌리므로,
   캐리포워드를 마친 `carried`를 기준으로 "완전히 빈 행"을 걸러 내면 **원래 텅 비어 있던 줄에 이미
   헤더 열 값이 채워져 있어서 유령 데이터 행으로 되살아난다.** CSV 중간의 빈 줄, 엑셀 `!ref`가 실제
   데이터보다 아래까지 잡힌 파일(행을 지운 적 있는 파일)에서 아주 흔하고, 그대로 두면 **원그래프와
   막대그래프(개수)의 숫자가 틀린다**(`lib/chart.js`의 pie/count 경로는 x값만 보고 세기 때문).
2. **캐리포워드의 값 비교는 `trim`한 것끼리 한다.** `"서울"`과 `"서울 "`을 다른 값으로 보면 상위 항목이
   안 바뀌었는데도 하위 열이 잘못 초기화되고, 그래프에서도 눈에 똑같아 보이는 항목이 둘로 갈라진다.
3. **열 개수를 셀 때 `Math.max(...배열)`을 쓰지 않는다.** 전개 연산자는 인자를 스택에 다 올려서
   행이 12만 개쯤 넘으면 `RangeError`로 죽는다. 20만 행 CSV가 6MB 남짓이라 이 앱의 20MB 한도를
   가볍게 통과한다. `maxRowLength()` 헬퍼를 쓸 것.

> ⚠️ 남은 한계: 행 수 자체의 상한이 없다. 15만 행짜리 파일을 올리면 위 `RangeError`는 안 나지만
> `renderGridInto`가 셀마다 `<input>`을 만들어 브라우저가 멈춘다. 수업용으로는 문제 없는 크기지만,
> 나중에 "N행까지만 받는다" 제한을 넣는 것이 좋다.

> ⚠️ 참고: 초기에 "공공데이터는 `data-cleaner`로 먼저 정리하면 된다"고 판단했지만 **이는 틀렸다.**
> `data-cleaner`는 원하는 행·열만 골라 내려받는 도구라서, 내려받은 파일의 헤더 구조는 원본 그대로다.
> (병합 헤더를 실제로 위>아래로 합쳐 주는 것은 `excel-picker` 쪽 기능이다.)

## ⚠️ 텍스트 입력에 절대 매 keystroke마다 상태를 반영하면 안 되는 이유

`main.js`의 `ctx.update()`는 `#app`을 통째로 비우고 다시 그린다(diffing 없는 단순 전체 재렌더).
`textarea`/`input[type=text]`의 `input` 이벤트마다 `ctx.update()`를 부르면 그 입력 칸 DOM이 매
글자마다 새로 만들어져 **포커스·커서 위치·한글 조합(IME) 상태를 잃어버린다.** 그래서 이 앱의 모든
텍스트 입력은:
- 입력 중에는 로컬 변수/DOM 값에만 담아 두고(포커스 유지),
- "다음" 버튼 활성/비활성 같은 가벼운 UI만 즉시 갱신하고,
- 실제 `ctx.update()`는 "다음"을 누르는 시점, blur, 또는 원래도 즉시 재렌더해도 되는 요소
  (체크박스·select·버튼)에서만 호출한다.

→ **새 입력 필드를 추가할 때 이 패턴을 반드시 따를 것.** `step2-collect.js`(표 셀 입력),
   `step1-topic.js`/`step4-analyze.js`(textarea)에 예시가 있다.

### 뒤집어 말하면 — `ctx.update()`를 부를 때는 **로컬 편집분을 반드시 함께 실어 보내야 한다**

`ctx.update()`는 `renderAll()`을 부르고, `step2-collect.js`의 `render()`는 처음부터 다시 실행되면서
`state.collection`에서 `columns`/`rows`를 새로 읽는다. 그래서 **state에 아직 반영 안 된 로컬 편집분은
그 순간 통째로 사라진다.** 2026-08-04에 실제로 이 결함이 두 군데 있었다:

- 「헤더 확인」의 **"이 설정으로 사용하기"** 가 로컬 변수에만 넣고 `ctx.update`를 안 불렀다
  → 그 뒤 수집 방법 칩을 누르거나 출처 칸에서 포커스가 빠지는 순간 **올린 파일이 통째로 사라졌다.**
- **출처 입력칸의 `blur`** 가 `{source}`만 넘겼다 → 표에 타이핑한 셀 값·열 이름이 전부 되돌아갔다.

→ `ctx.update({ collection: { ... } })`를 부를 때는 항상 `columns, rows`를 함께 넘긴다.
   (`state.collection.methods`처럼 이미 state에 있는 값은 얕은 병합이 알아서 지켜 준다)

## 「고르기」는 왜 지우지 않고 체크로 하는가 (2026-08-04 추가)

2단계에서 원하는 열·행만 골라 다음으로 넘기는 기능은 **원본 표를 건드리지 않는다.**
`state.collection` 에는 표 전체(`columns`/`rows`)가 그대로 있고, 무엇을 골랐는지는
**같은 길이의 boolean 배열**(`colSelected`/`rowSelected`)로 따로 기억한다.
3·4·5단계는 `lib/select.js` 의 `applySelection(collection)` 이 걸러 준 표만 본다.

- **왜 boolean 배열인가**: 번호(인덱스) 목록으로 두면 열·행을 추가·삭제할 때마다 번호가 밀려
  어긋난다. 길이가 같은 배열이면 `splice`/`filter` 를 나란히 해 주면 끝이다.
- **왜 지우지 않는가**: 「행 삭제」는 되돌릴 수 없다. 체크는 껐다 켰다 할 수 있고, 검색과 함께
  쓰면 수십 줄을 한 번에 고를 수 있다. 학생이 2단계로 되돌아오면 원본이 그대로 남아 다시 고를 수 있다.
- **`applySelection` 호출처가 4곳이다** — `step3-visualize.js`, `step4-analyze.js`,
  `step5-report.js`, `lib/report.js`(내려받는 보고서 파일). **새로 표를 읽는 곳을 만들면 여기도 추가해야 한다.**
  안 하면 화면 그래프와 보고서 표의 내용이 서로 달라진다.
- 「보이는 행 모두 체크/해제」는 **검색으로 걸러져 보이는 행에만** 적용한다(`excel-picker` 와 같은 규칙).
  숨겨진 것까지 건드리면 "검색해서 몇 개만 켜기"가 불가능해진다.
- 파일을 새로 올려 「이 설정으로 사용하기」를 누르면 표 크기가 바뀌므로 **선택을 전부 true 로 새로 만든다.**

### 탭 상태(`activeTab`)가 모듈 수준에 있는 이유

같은 이유다. `render()` 안의 지역 변수로 두면 `ctx.update()`가 일어날 때마다(예: "+ 행 추가")
`'file'`로 초기화되어 **「표에 직접 입력」 탭에서 행을 추가할 때마다 「파일 업로드」 탭으로 튕겨 나갔다.**
그래서 `activeTab`만 모듈 수준 변수로 빼 두었다. state에는 넣지 않는다(순수 화면 상태라 저장할 이유가 없다).

## 개인정보

학생이 입력한 탐구 질문·수집 데이터·분석 답변은 **이 컴퓨터의 브라우저 `localStorage`에만**
있다(`data-report-state-v1`). 서버 전송 없음, 로그인 없음.

**학번·이름은 예외적으로 입력칸이 있다** (`step5-report.js`의 `#studentIdInput`/`#studentNameInput`,
2026-08-03 사용자 요청으로 추가). 하지만 **`ctx.update()`를 거치지 않는 순수 로컬 변수**라서
`state`에 들어가지 않고, 따라서 `localStorage`에도 저장되지 않는다 — 새로고침하거나 다른 단계로
갔다 오면 사라진다. "보고서 파일로 저장"을 누르는 그 순간에만 값을 읽어 `buildReportHtml()`의
세 번째 인자로 전달되어 보고서 파일 안에 찍힌다. 이렇게 만든 이유: 학번·이름을 `state`에 넣고
저장하면 공용 컴퓨터(교실 PC)에서 다음 학생이 이전 학생의 이름을 그대로 보게 되는 문제가 생긴다.
**이 "저장하지 않는" 성질은 바꾸지 말 것** — 학번·이름을 `state`/`localStorage`로 옮기고 싶어지면
먼저 공용 컴퓨터 시나리오와 프로젝트 규칙(학생 개인정보 수집·저장 금지)을 다시 확인해야 한다.

## 파일

| 파일 | 하는 일 |
|:---|:---|
| `index.html` | `<div id="app">` 하나뿐인 뼈대. 나머지는 `main.js`가 그린다 |
| `style.css` | 2단원 색(청록 `#12b886`, hub 메인페이지와 통일). 본문 19px, 카드/버튼 큼직하게 |
| `main.js` | 위저드 조립: 진행 표시줄, 현재 단계에 맞는 `steps/*.js`의 `render()` 호출, `ctx`(state/update/goNext/goBack/goToStep/resetAll) 제공 |
| `lib/state.js` | 전체 상태 모양, 상수(수집 방법·그래프 종류·분석 질문·성취기준·예시), `localStorage` 저장/복원 |
| `lib/util.js` | 숫자 판별·형 추정·HTML 이스케이프 등 여러 파일이 공유하는 순수 함수 |
| `lib/io.js` | 파일(csv/xlsx/xls) 파싱, CP949/UTF-8 인코딩 자동 판별. `readTable()`은 1행을 헤더로 보고 바로 `{columns, rows}`를, `readRawGrid()`는 헤더를 정하지 않은 채 병합만 푼 `{grid}`를 돌려준다(「헤더 확인」 화면이 이걸 쓴다) |
| `lib/header-merge.js` | `unmergeGrid()` 병합 칸 펴기 · `carryForwardCols()` 헤더 열의 생략된 값 이어 채우기 · `mergeHeaderRows(grid, 행수, {sep, headerColCount})` 열 이름 합치기. **DOM을 쓰지 않는 순수 함수**라 Node로 바로 시험한다 |
| `lib/select.js` | 「체크한 열·행만 골라내기」 — `applySelection()` · `selectionCounts()` · `rowMatchesQuery()`(행 검색) · `normalizeSelection()`. 역시 순수 함수 |
| `lib/chart.js` | `collection`+`visualization` → Chart.js 설정으로 변환(`buildChartConfig`), canvas에 그리기(`renderChart`) |
| `lib/report.js` | 상태 전체 → 자체완결형 보고서 HTML 문자열(`buildReportHtml`), 파일 다운로드(`downloadReportHtml`) |
| `steps/step0-intro.js` ~ `step5-report.js` | 단계별 화면. 서로 import하지 않고 `main.js`가 주는 `ctx`로만 소통한다. **`step2-collect.js`만 화면이 둘**(평소 수집 화면 / 파일 업로드 직후의 「헤더 확인」 화면)이며, 그 전환은 `main.js`의 단계 이동이 아니라 이 파일 안의 지역 변수 `mode`로 관리한다 — `state`에 들어가지 않는다 |
| `tests/*.test.mjs` | 프레임워크 없는 순수 Node 테스트 (data-cleaner와 같은 스타일). `npm test`로 6개 파일 순서대로 실행 |
| `vite.config.js` | 포트 8087, `base: './'` |
